products-service-dev:
  params:
    x: 901
    y: 901

spring:
  application:
    name: products-service

  datasource:
    url: jdbc:postgresql://localhost:5432/product
    username: postgres
    password: lamaachi

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  feign:
    client:
      config:
        default:
          # Optional: Customize Feign client settings
          connectTimeout: 5000
          readTimeout: 5000
  customers-service:
    url: http://localhost:8082  # or your actual service URL
server:
  port: 8082
