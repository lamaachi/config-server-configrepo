orders-service-dev:
  params:
    x: 6574
    y: 5235

spring:
  application:
    name: orders-service

  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: postgres
    password: lamaachi

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  feign:
    client:
      config:
        default:
          # Optional: Customize Feign client settings
          connectTimeout: 5000
          readTimeout: 5000
  customers-service:
    url: http://localhost:8083  # or your actual service URL
server:
  port: 8083
